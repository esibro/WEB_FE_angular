import { CourseFormErrorMessages } from './course-form-error-messages';

describe('CourseFormErrorMessages', () => {
  it('should create an instance', () => {
    expect(new CourseFormErrorMessages()).toBeTruthy();
  });
});
